{
  "name": "Mistral — Streaming + Reasoning",
  "description": "Connect to the Mistral API with streaming and native reasoning via Magistral models. Reasoning is always active — no extra parameters needed. Reasoning traces appear before the final answer.",
  "requiresStreaming": true,
  "fields": [
    {
      "key": "api_key",
      "label": "Mistral API Key",
      "type": "text",
      "placeholder": "your-mistral-api-key",
      "inputType": "password"
    },
    {
      "key": "model",
      "label": "Model",
      "type": "select",
      "options": [
        { "value": "magistral-small-latest", "label": "Magistral Small" },
        { "value": "magistral-medium-latest", "label": "Magistral Medium" }
      ],
      "default": "magistral-small-latest"
    }
  ],
  "connector": {
    "version": "1",
    "request": {
      "url": "https://api.mistral.ai/v1/chat/completions",
      "method": "POST",
      "auth": {
        "type": "bearer",
        "token": "{{api_key}}"
      },
      "headers": {
        "Content-Type": "application/json",
        "Accept": "text/event-stream"
      },
      "timeoutMs": 60000,
      "stream": true
    },
    "aiPayload": {
      "model": "{{model}}",
      "temperature": 0.7,
      "max_tokens": 4096,
      "stream": true
    },
    "history": {
      "mode": "array",
      "extractSystem": false
    },
    "response": {
      "resultPath": "$.choices[0].message.content",
      "stream": {
        "linePrefix": "data:",
        "doneSignal": "[DONE]",
        "extractors": [
          { "tokenPath": "$.choices[0].delta.content" }
        ]
      }
    }
  }
}
